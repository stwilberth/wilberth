---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../../consts';
import { getCollection } from 'astro:content';
import FormattedDate from '../../components/FormattedDate.astro';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<!doctype html>
<html lang="es">
	<head>
		<BaseHead 
			title={SITE_TITLE} 
			subtitle='Blog'
			description="Artículos sobre desarrollo web, tecnología y consejos para tu negocio digital" 
		/>
		<style>
			main {
				max-width: 1200px;
				margin: 0 auto;
				padding: 2rem;
			}
			.blog-header {
				text-align: center;
				margin-bottom: 3rem;
				padding: 2rem 0;
				background: linear-gradient(135deg, rgba(var(--accent-light), 0.1), rgba(var(--accent-dark), 0.1));
				border-radius: 1rem;
			}
			.blog-header h1 {
				font-size: 2.5rem;
				color: var(--accent);
				margin-bottom: 1rem;
			}
			.blog-header p {
				color: rgb(var(--gray));
				max-width: 600px;
				margin: 0 auto;
			}
			ul {
				display: grid;
				grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
				gap: 2rem;
				list-style-type: none;
				margin: 0;
				padding: 0;
			}
			ul li {
				background: white;
				border-radius: 1rem;
				overflow: hidden;
				box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
				transition: transform 0.3s ease;
			}
			ul li:hover {
				transform: translateY(-5px);
			}
			ul li img {
				width: 100%;
				aspect-ratio: 16/9;
				object-fit: cover;
			}
			ul li a {
				text-decoration: none;
				color: inherit;
			}
			.content {
				padding: 1.5rem;
			}
			.title {
				margin: 0;
				color: rgb(var(--black));
				line-height: 1.4;
				font-size: 1.2rem;
			}
			.date {
				margin: 0.5rem 0 0;
				color: rgb(var(--gray));
				font-size: 0.9rem;
			}
			@media (max-width: 768px) {
				main {
					padding: 1rem;
				}
				ul {
					gap: 1rem;
				}
				.blog-header h1 {
					font-size: 2rem;
				}
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<div class="blog-header">
				<h1>Blog</h1>
				<p>Artículos sobre desarrollo web, tecnología y consejos para tu negocio digital</p>
			</div>
			<section>
				<ul>
					{
						posts.map((post) => (
							<li>
								<a href={`/blog/${post.id}`}>
									<img width={720} height={360} src={post.data.heroImage} alt="" />
									<div class="content">
										<h2 class="title">{post.data.title}</h2>
										<p class="date">
											<FormattedDate date={post.data.pubDate} />
										</p>
									</div>
								</a>
							</li>
						))
					}
				</ul>
			</section>
		</main>
		<Footer />
	</body>
</html>
