---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Logo from '../components/Logo.astro';
import WhatsAppButton from '../components/WhatsAppButton.astro';
import { SITE_TITLE, SITE_SUBTITLE, SITE_DESCRIPTION, SITE_PHONE_NUMBER } from '../consts';
import PricingLink from '../components/PricingLink.astro';
---

<!doctype html>
<html lang="es">
	<head>
		<BaseHead title='Diseño web, email marketing y SEO' subtitle={SITE_SUBTITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<h1>{SITE_SUBTITLE}</h1>
			<p>{SITE_DESCRIPTION}</p>
			
			<div class="button-container">
				<WhatsAppButton phoneNumber={SITE_PHONE_NUMBER} text="Contactar" />
				<!-- ver precios button -->
				<PricingLink text='Ver Precios' />
			</div>
			

			<div class="features-grid">
				<!-- rendimiento optimizado  -->
				<div class="feature-card">
					<span class="icon">⚡</span>
					<h3>Rendimiento Optimizado</h3>
					<p>Páginas web ultrarrápidas con tiempos de carga optimizados para una mejor experiencia de usuario.</p>
				</div>
				<!-- seo avanzado -->
				<div class="feature-card">
					<span class="icon">🔍</span>
					<h3>SEO Avanzado</h3>
					<p>Implementación de las mejores prácticas de SEO para maximizar la visibilidad en buscadores.</p>
				</div>
				<!-- diseño responsivo -->
				<div class="feature-card">
					<span class="icon">📱</span>
					<h3>Diseño Responsivo</h3>
					<p>Interfaces adaptables que funcionan perfectamente en todos los dispositivos y tamaños de pantalla.</p>
				</div>
				<!-- seguridad robusta -->
				<div class="feature-card">
					<span class="icon">🛡️</span>
					<h3>Seguridad Robusta</h3>
					<p>Implementación de protocolos de seguridad avanzados para proteger tu sitio y tus usuarios.</p>
				</div>
				<!-- soporte multilenguaje -->
				<div class="feature-card">
					<span class="icon">🌐</span>
					<h3>Soporte Multilenguaje</h3>
					<p>Soporte multilenguaje para garantizar que tu sitio sea accesible a todos los usuarios.</p>
				</div>
				<!-- inteligencia artificial  -->
				<div class="feature-card">
					<span class="icon">🤖</span>
					<h3>Inteligencia Artificial</h3>
					<p>Integración de inteligencia artificial para mejorar la experiencia del usuario.</p>
				</div>
			</div>


			<style>
				
				.button-container {
					display: flex;
					gap: 1rem;
					justify-content: center;
					align-items: center;
					margin: 1rem 0;
				}
				.features-grid {
					display: grid;
					grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
					gap: 2rem;
					margin: 3rem 0;
				}

				.feature-card {
					background: #ffffff;
					border-radius: 10px;
					padding: 1.5rem;
					box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
					transition: transform 0.3s ease;
				}

				.feature-card:hover {
					transform: translateY(-5px);
				}

				.icon {
					font-size: 2rem;
					display: inline-block;
					margin-right: 0.5rem;
					vertical-align: middle;
				}

				.feature-card h3 {
					display: inline-block;
					margin: 0;
					color: #333;
					font-size: 1.25rem;
					vertical-align: middle;
				}

				.feature-card p {
					margin: 1rem 0 0 0;
					color: #666;
					font-size: 0.9rem;
					line-height: 1.5;
				}

			</style>
		</main>
		<Footer />
	</body>
</html>
