---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Logo from '../components/Logo.astro';
import PricingWeb from '../components/PricingWeb.astro';
import PricingEmail from '../components/PricingEmail.astro';
import WhatsAppButton from '../components/WhatsAppButton.astro';
import { SITE_TITLE, SITE_SUBTITLE, SITE_DESCRIPTION, SITE_PHONE_NUMBER } from '../consts';
---

<!doctype html>
<html lang="es">
	<head>
		<BaseHead title={SITE_TITLE} subtitle={SITE_SUBTITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<h1>{SITE_TITLE}</h1>
			<h2>{SITE_SUBTITLE}</h2>
			<p>{SITE_DESCRIPTION}</p>
			
			<WhatsAppButton phoneNumber={SITE_PHONE_NUMBER} text="¡Contáctanos!" />

			<div class="features-grid">
				<div class="feature-card">
					<span class="icon">⚡</span>
					<h3>Rendimiento Optimizado</h3>
					<p>Páginas web ultrarrápidas con tiempos de carga optimizados para una mejor experiencia de usuario.</p>
				</div>
				<div class="feature-card">
					<span class="icon">🔍</span>
					<h3>SEO Avanzado</h3>
					<p>Implementación de las mejores prácticas de SEO para maximizar la visibilidad en buscadores.</p>
				</div>
				<div class="feature-card">
					<span class="icon">📱</span>
					<h3>Diseño Responsivo</h3>
					<p>Interfaces adaptables que funcionan perfectamente en todos los dispositivos y tamaños de pantalla.</p>
				</div>
				<div class="feature-card">
					<span class="icon">🛡️</span>
					<h3>Seguridad Robusta</h3>
					<p>Implementación de protocolos de seguridad avanzados para proteger tu sitio y tus usuarios.</p>
				</div>
			</div>

			<PricingWeb />

			<style>
				.features-grid {
					display: grid;
					grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
					gap: 2rem;
					margin: 3rem 0;
				}

				.feature-card {
					background: #ffffff;
					border-radius: 10px;
					padding: 1.5rem;
					box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
					transition: transform 0.3s ease;
				}

				.feature-card:hover {
					transform: translateY(-5px);
				}

				.icon {
					font-size: 2rem;
					display: inline-block;
					margin-right: 0.5rem;
					vertical-align: middle;
				}

				.feature-card h3 {
					display: inline-block;
					margin: 0;
					color: #333;
					font-size: 1.25rem;
					vertical-align: middle;
				}

				.feature-card p {
					margin: 1rem 0 0 0;
					color: #666;
					font-size: 0.9rem;
					line-height: 1.5;
				}

			</style>
		</main>
		<Footer />
	</body>
</html>
